<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.latin.cwbastardi.latin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/roman_numeral_seven"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/roman_numeral_seven"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BodyActivity" />
        <activity android:name=".PeopleActivity" />
        <activity android:name=".NatureActivity" />
        <activity android:name=".NumbersActivity" />
        <activity android:name=".VerbsActivity"></activity>
    </application>

</manifest>